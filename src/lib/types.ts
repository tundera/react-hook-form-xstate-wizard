export type ContactData = {
  name: string;
  email: string;
  phone: string;
};

export type OrderData = {
  flavor: string;
  size: string;
};

export type AdditionalDetailsData = {
  details: string;
};
